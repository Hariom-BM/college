React Tutorial Day 2

- Run `npm install` once in this folder to install dependencies.
- Do not commit the `node_modules/` folder.
- `package-lock.json` is included so installs stay consistent.
- Use `npm install` again only if `package.json` or `package-lock.json` changes.
